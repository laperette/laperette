// import { FormField, TextInput, Button } from "grommet";
import React from "react";
// import { useForm } from "react-hook-form";

export const NewBooking = () => {
  // const { register, handleSubmit, errors } = useForm();

  // const onSubmit: OnSubmit = (data) => {
  //   login(data);
  // };

  return (
    <h1>HELLO NEW BOOKING</h1>
    // <form onSubmit={() => console.log("Hello")}>
    //   <FormField htmlFor="email" label="Email" error={errors}>
    //     <TextInput
    //       autoComplete="email"
    //       id="email"
    //       name="email"
    //       placeholder="email"
    //       type="email"
    //       ref={register({ required: true })}
    //     />
    //   </FormField>
    //   <FormField htmlFor="password" label="Mot de passe" error={}>
    //     <TextInput
    //       autoComplete="current-password"
    //       id="password"
    //       name="password"
    //       placeholder="password"
    //       type="password"
    //       ref={register({ required: true })}
    //     />
    //   </FormField>
    //   <Button type="submit" primary label="Log me in" />
    // </form>
  );
};
